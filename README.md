# 广告平台管理系统

基于 Vue 3 + TypeScript + Element Plus 的现代化管理后台系统。

## 功能特性

### 🎯 核心功能
- **用户认证** - JWT登录、权限控制、角色管理
- **产品管理** - 产品CRUD、分类管理、库存管理
- **计划管理** - 广告计划创建、投放控制、效果统计
- **优惠券管理** - 优惠券发放、使用统计、用户管理
- **授权码管理** - 授权码生成、使用跟踪、权限控制
- **财务管理** - 交易记录、充值提现、财务统计
- **客户管理** - 用户信息、等级管理、行为分析
- **系统设置** - 配置管理、管理员管理、系统监控
- **数据统计** - 多维度报表、图表分析、数据导出

### 🎨 界面特色
- **现代化设计** - Material Design 风格，专业美观
- **响应式布局** - 完美适配桌面、平板、手机设备
- **主题定制** - 支持亮色/暗色主题，主题色自定义
- **交互优化** - 流畅的动画效果，直观的用户体验

### 🛠 技术栈
- **前端框架**: Vue 3.4 + TypeScript
- **构建工具**: Vite 5.2
- **UI组件**: Element Plus 2.6
- **状态管理**: Pinia 2.1
- **路由管理**: Vue Router 4.3
- **图表组件**: ECharts 5.5
- **HTTP客户端**: Axios 1.6
- **样式预处理**: Sass

## 快速开始

### 环境要求
- Node.js >= 16.0.0
- npm >= 7.0.0

### 安装依赖
```bash
npm install
```

### 开发环境
```bash
npm run dev
```
访问 http://localhost:3000

### 构建生产
```bash
npm run build
```

### 类型检查
```bash
npm run type-check
```

### 代码检查
```bash
npm run lint
```

## 项目结构

```
src/
├── api/              # API接口
├── components/       # 通用组件
│   ├── charts/       # 图表组件
│   ├── forms/        # 表单组件
│   └── upload/       # 上传组件
├── directives/       # 自定义指令
├── layouts/          # 布局组件
│   └── components/   # 布局子组件
├── router/           # 路由配置
├── stores/           # 状态管理
├── styles/           # 全局样式
├── types/            # 类型定义
├── utils/            # 工具函数
├── views/            # 页面组件
│   ├── dashboard/    # 仪表盘
│   ├── products/     # 产品管理
│   ├── campaigns/    # 计划管理
│   ├── coupons/      # 优惠券管理
│   ├── authcodes/    # 授权码管理
│   ├── finance/      # 财务管理
│   ├── customers/    # 客户管理
│   ├── system/       # 系统管理
│   └── statistics/   # 数据统计
├── App.vue           # 根组件
└── main.ts           # 入口文件
```

## 核心配置

### 环境变量
```env
# 开发环境 (.env.development)
VITE_API_BASE_URL=http://localhost:8000/api
VITE_APP_TITLE=广告平台管理系统
VITE_APP_VERSION=1.0.0

# 生产环境 (.env.production)
VITE_API_BASE_URL=https://api.yourdomain.com/api
VITE_APP_TITLE=广告平台管理系统
VITE_APP_VERSION=1.0.0
```

### 权限系统
系统内置基于角色的权限控制：
- **admin**: 超级管理员，拥有所有权限
- **operator**: 操作员，拥有日常操作权限
- **viewer**: 查看者，仅有查看权限

```typescript
// 使用权限指令
<el-button v-permission="'product:create'">创建产品</el-button>
<el-button v-permission="['product:edit', 'product:delete']">编辑</el-button>

// 在代码中检查权限
import { hasPermission } from '@/utils/permission'
if (hasPermission('product:create')) {
  // 有权限的逻辑
}
```

### API请求
```typescript
import { request } from '@/utils/request'

// GET请求
const response = await request.get('/api/products')

// POST请求
const response = await request.post('/api/products', data)

// 文件上传
const response = await request.upload('/api/upload', file)
```

## 页面功能

### 📊 仪表盘
- 核心数据统计卡片
- 收入趋势图表
- 用户增长图表
- 最近活动列表

### 📦 产品管理
- 产品列表（搜索、筛选、排序）
- 产品创建/编辑表单
- 产品详情展示
- 批量操作（删除、状态更新）
- 数据导出功能

### 🎯 计划管理
- 计划列表管理
- 计划创建向导
- 效果统计报表
- 状态控制（启动、暂停、完成）

### 🎟 优惠券管理
- 优惠券发放管理
- 使用记录追踪
- 用户优惠券查询

### 🔑 授权码管理
- 批量生成授权码
- 使用状态跟踪
- 权限配置

### 💰 财务管理
- 交易记录查询
- 充值提现管理
- 财务统计报表

### 👥 客户管理
- 客户信息管理
- 等级体系配置
- 行为数据分析

### ⚙️ 系统管理
- 系统配置管理
- 管理员账户管理
- 权限角色配置

### 📈 数据统计
- 多维度数据分析
- 可视化图表展示
- 数据导出功能

## 开发指南

### 添加新页面
1. 在 `src/views/` 下创建页面组件
2. 在 `src/router/index.ts` 配置路由
3. 在 `src/stores/permission.ts` 添加菜单配置

### 添加新API
1. 在 `src/api/` 下创建API模块
2. 在 `src/types/index.ts` 定义类型
3. 在页面中使用API

### 权限配置
1. 在 `src/utils/permission.ts` 定义权限常量
2. 在路由和菜单配置中添加权限要求
3. 在组件中使用权限指令或函数

## 部署说明

### 构建优化
- 自动代码分割
- 图片资源优化
- Gzip压缩支持
- CDN资源配置

### 生产配置
- 环境变量配置
- API接口地址
- 静态资源路径
- 缓存策略设置

## 浏览器支持

- Chrome >= 88
- Firefox >= 85  
- Safari >= 14
- Edge >= 88

## 更新日志

### v1.0.0 (2024-08-28)
- ✨ 初始版本发布
- 🎨 完整的管理后台功能
- 📱 响应式设计
- 🔐 权限控制系统
- 📊 数据统计功能
- 🎯 多模块管理
- 🌙 主题切换支持

## 技术支持

如有问题或建议，请通过以下方式联系：
- 邮箱: support@example.com
- 文档: https://docs.example.com
- GitHub: https://github.com/your-repo

## 许可证

[MIT License](./LICENSE)